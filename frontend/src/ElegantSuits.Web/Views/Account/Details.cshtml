@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model ElegantSuits.Web.Models.UserDetailsViewModel

@{
    ViewData["Title"] = "Th�ng tin ngu?i d�ng";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-8">
        @if (!string.IsNullOrEmpty(Model.AvatarUrl))
        {
            <div class="mb-3">
                <img src="@Model.AvatarUrl" alt="Avatar" class="rounded-circle" style="width: 150px; height: 150px; object-fit: cover;" />
            </div>
        }
        <dl class="row">
            <dt class="col-sm-4">Email</dt>
            <dd class="col-sm-8">@Model.Email</dd>

            <dt class="col-sm-4">H? v� t�n</dt>
            <dd class="col-sm-8">@Model.FullName</dd>

            <dt class="col-sm-4">Ng�y sinh</dt>
            <dd class="col-sm-8">@Model.DateOfBirth.ToString("dd/MM/yyyy")</dd>

            <dt class="col-sm-4">S? di?n tho?i</dt>
            <dd class="col-sm-8">@(Model.PhoneNumber ?? "Chua c?p nh?t")</dd>

            <dt class="col-sm-4">�?a ch?</dt>
            <dd class="col-sm-8">@(Model.Address ?? "Chua c?p nh?t")</dd>

            <dt class="col-sm-4">Ng�y t?o t�i kho?n</dt>
            <dd class="col-sm-8">@Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</dd>
        </dl>

        <div class="form-group">
            @if (User.IsInRole("Administrator") && User.Identity?.Name != Model.Email)
            {
                <a asp-action="UpdateUser" asp-route-id="@Model.Id" class="btn btn-primary">C?p nh?t th�ng tin</a>
                <a asp-action="Index" class="btn btn-secondary">Quay l?i danh s�ch</a>
            }
            else
            {
                <a asp-action="Update" class="btn btn-primary">C?p nh?t th�ng tin</a>
                @if (User.IsInRole("Administrator"))
                {
                    <a asp-action="Index" class="btn btn-secondary">Quay l?i danh s�ch</a>
                }
            }
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
