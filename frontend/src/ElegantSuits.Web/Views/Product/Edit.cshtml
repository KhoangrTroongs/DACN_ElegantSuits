@model ElegantSuits.Web.Models.ProductViewModel
@using Microsoft.AspNetCore.Mvc.Rendering
@{
    ViewData["Title"] = "S?a s?n ph?m";
}

<h1>S?a</h1>

<h4>S?n ph?m</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="ExistingImageUrl" />
            <input type="hidden" asp-for="ExistingModel3DUrl" />
            <div class="form-group mb-3">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Description" class="control-label"></label>
                @{
                    var description = Model.Description;
                    var sizeTag = "[SIZES]";
                    var endSizeTag = "[/SIZES]";
                    var reviewTag = "[REVIEWS]";
                    var endReviewTag = "[/REVIEWS]";

                    // Lo?i b? ph?n k? thu?t kh?i m� t?
                    if (!string.IsNullOrEmpty(description))
                    {
                        // Lo?i b? ph?n k�ch thu?c
                        if (description.Contains(sizeTag) && description.Contains(endSizeTag))
                        {
                            var startIndex = description.IndexOf(sizeTag);
                            var endIndex = description.IndexOf(endSizeTag) + endSizeTag.Length;
                            if (startIndex < endIndex)
                            {
                                description = description.Remove(startIndex, endIndex - startIndex);
                            }
                        }

                        // Lo?i b? ph?n d�nh gi�
                        if (description.Contains(reviewTag) && description.Contains(endReviewTag))
                        {
                            var startIndex = description.IndexOf(reviewTag);
                            var endIndex = description.IndexOf(endReviewTag) + endReviewTag.Length;
                            if (startIndex < endIndex)
                            {
                                description = description.Remove(startIndex, endIndex - startIndex);
                            }
                        }

                        // Lo?i b? kho?ng tr?ng th?a
                        description = description.Trim();
                    }
                }
                <textarea asp-for="Description" class="form-control" rows="3">@description</textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
                <small class="form-text text-muted">M� t? s?n ph?m. Th�ng tin v? k�ch thu?c s? du?c qu?n l� ri�ng.</small>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Price" class="control-label"></label>
                <input asp-for="Price" class="form-control" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Quantity" class="control-label"></label>
                <div class="input-group">
                    <input asp-for="Quantity" class="form-control" type="number" min="0" />
                    <a asp-action="ManageProductSizes" asp-route-id="@Model.Id" class="btn btn-outline-primary">
                        <i class="fas fa-tshirt"></i> Qu?n l� k�ch c?
                    </a>
                </div>
                <span asp-validation-for="Quantity" class="text-danger"></span>
                <small class="form-text text-muted">S? lu?ng t?ng c?a t?t c? c�c k�ch c?. S? d?ng n�t "Qu?n l� k�ch c?" d? th�m/s?a/x�a k�ch c?.</small>
            </div>
            <div class="form-group mb-3">
                <label asp-for="LinearCode" class="control-label"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-barcode"></i></span>
                    <input asp-for="LinearCode" class="form-control" placeholder="Nh?p m� 12 s?" maxlength="12" pattern="\d{12}" />
                </div>
                <span asp-validation-for="LinearCode" class="text-danger"></span>
                <small class="form-text text-muted">M� Linear (Barcode) 12 ch? s? d�ng d? qu�t m� khi nh?p/xu?t kho.</small>
            </div>
            <div class="form-group mb-3">
                <label asp-for="CategoryId" class="control-label"></label>
                <select asp-for="CategoryId" class="form-control" asp-items="@(new SelectList(Model.Categories, "Id", "Name"))">
                    <option value="">-- Ch?n danh m?c --</option>
                </select>
                <span asp-validation-for="CategoryId" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label class="control-label">V?i (kh�ng b?t bu?c)</label>
                <div class="border rounded p-3" style="max-height: 300px; overflow-y: auto;">
                    @if (Model.Fabrics != null && Model.Fabrics.Any())
                    {
                        @foreach (var fabric in Model.Fabrics.OrderBy(f => f.FabricGroupName).ThenBy(f => f.Name))
                        {
                            var isSelected = Model.SelectedFabricIds != null && Model.SelectedFabricIds.Contains(fabric.Id);
                            <div class="form-check">
                                <input type="checkbox" class="form-check-input" id="fabric_@fabric.Id"
                                       name="SelectedFabricIds" value="@fabric.Id" @(isSelected ? "checked" : "") />
                                <label class="form-check-label" for="fabric_@fabric.Id">
                                    @fabric.Name
                                    @if (!string.IsNullOrEmpty(fabric.FabricGroupName))
                                    {
                                        <span class="text-muted">(@fabric.FabricGroupName)</span>
                                    }
                                </label>
                            </div>
                        }
                    }
                    else
                    {
                        <p class="text-muted">Kh�ng c� v?i n�o. Vui l�ng t?o v?i tru?c.</p>
                    }
                </div>
                <small class="form-text text-muted">Ch?n c�c v?i c� s?n cho s?n ph?m n�y. Kh�ch h�ng s? c� th? ch?n t? c�c v?i n�y khi thi?t k?.</small>
            </div>

            <div class="form-group mb-3">
                <div class="form-check">
                    <input asp-for="IsHidden" class="form-check-input" />
                    <label asp-for="IsHidden" class="form-check-label">?n s?n ph?m</label>
                </div>
                <small class="form-text text-muted">Khi du?c ch?n, s?n ph?m s? kh�ng hi?n th? cho kh�ch h�ng.</small>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Image" class="control-label"></label>
                <input asp-for="Image" type="file" class="form-control" accept="image/*" />
                <span asp-validation-for="Image" class="text-danger"></span>
            </div>

<div class="form-group mb-3">
    <label class="control-label">H�nh ?nh hi?n t?i</label>
    <div>
        @if (!string.IsNullOrEmpty(Model.ExistingImageUrl))
        {
            <img src="@Model.ExistingImageUrl" alt="H�nh ?nh hi?n t?i" style="max-width: 200px; max-height: 200px;" />
            <div class="d-flex justify-content-between align-items-center mt-2">
                <span class="text-muted">T?i l�n h�nh ?nh m?i d? thay th? h�nh ?nh hi?n t?i</span>
                <a asp-action="DeleteImageGet" asp-route-id="@Model.Id" class="btn btn-danger btn-sm" onclick="return confirm('B?n c� ch?c ch?n mu?n x�a h�nh ?nh n�y?');">
                    <i class="fas fa-trash-alt"></i> X�a h�nh ?nh
                </a>
            </div>
        }
        else
        {
            <img src="/images/no-image.svg" alt="Kh�ng c� h�nh ?nh" style="max-width: 200px; max-height: 200px;" onerror="this.src='https://via.placeholder.com/200x200?text=No+Image'" />
            <div class="mt-2">
                <span class="text-muted">S?n ph?m chua c� h�nh ?nh. Vui l�ng t?i l�n h�nh ?nh.</span>
            </div>
        }
    </div>
</div>

            <div class="form-group mb-3">
                <label asp-for="Model3D" class="control-label">M� h�nh 3D (kh�ng b?t bu?c)</label>
                <div class="input-group">
                    <input asp-for="Model3D" type="file" class="form-control" accept=".glb,.gltf,.obj" />
                    <a href="http://localhost:5678/form-test/3cf7e768-b69e-4347-9292-567ce14e32ab" target="_blank" class="btn btn-info">
                        <i class="fas fa-cube"></i> T?o Model 3D
                    </a>
                </div>
                <span asp-validation-for="Model3D" class="text-danger"></span>
            </div>

@if (!string.IsNullOrEmpty(Model.ExistingModel3DUrl))
{
    <div class="form-group mb-3">
        <label class="control-label">M� h�nh 3D hi?n t?i</label>
        <div>
            <p><i class="fas fa-cube"></i> @System.IO.Path.GetFileName(Model.ExistingModel3DUrl)</p>
        </div>
        <div class="d-flex justify-content-between align-items-center mt-2">
            <span class="text-muted">T?i l�n m� h�nh 3D m?i d? thay th? m� h�nh hi?n t?i</span>
            <a asp-action="DeleteModel3DGet" asp-route-id="@Model.Id" class="btn btn-danger btn-sm" onclick="return confirm('B?n c� ch?c ch?n mu?n x�a m� h�nh 3D n�y?');">
                <i class="fas fa-trash-alt"></i> X�a m� h�nh 3D
            </a>
        </div>
    </div>
}

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Luu</button>
                <a asp-action="Index" class="btn btn-secondary">Quay l?i danh s�ch</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
